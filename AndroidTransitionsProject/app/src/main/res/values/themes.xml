<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ContentTransitionAppTheme" parent="Theme.AppCompat.Light">
        <!-- Enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>
        <!-- Specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/slide</item>
        <item name="android:windowExitTransition">@transition/slide</item>

        <item name="android:windowReturnTransition">@transition/slide</item>
        <item name="android:windowReenterTransition">@transition/slide</item>
    </style>

    <style name="ContentTransitionAppTheme.AutoTransition">
        <!-- Specify enter and exit transitions -->
        <item name="android:windowEnterTransition">@transition/auto_transition</item>
        <item name="android:windowExitTransition">@transition/auto_transition</item>
        <item name="android:windowReturnTransition">@transition/auto_transition</item>
        <item name="android:windowReenterTransition">@transition/auto_transition</item>
    </style>

    <style name="SharedElementsTheme" parent="Theme.AppCompat.Light">
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
    </style>
</resources>