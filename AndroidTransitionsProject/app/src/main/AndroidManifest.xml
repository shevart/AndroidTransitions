<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shevart.androidtransitions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".scene.SceneTopicActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".scene.samples.searchbar.SearchBarSceneSampleActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".scene.samples.bylayoutid.ContentByLayoutIdSceneSampleActivity"></activity>
    </application>

</manifest>